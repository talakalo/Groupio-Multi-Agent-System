---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/databases/postgres.py
  - src/services/whatsapp_bot.py
  - src/api/routes/webhooks.py
  - .env.example
  - docker/staging-minimal.env
autonomous: true
requirements:
  - INFRA-01
  - OPS-01
tags:
  - infra
  - whatsapp
  - supabase
  - stripe
  - env-vars
must_haves:
  truths:
    - "FastAPI asyncpg pool can connect to a Supavisor transaction pooler (port 6543) without prepared-statement errors"
    - "All three WhatsApp Graph API call sites use the same current API version (v21.0)"
    - ".env.example documents the Supavisor pooler connection string at port 6543, the live Stripe key placeholders, and the 7-day payout delay"
    - "docker/staging-minimal.env clarifies that local Docker Postgres uses port 5432 (not the Supavisor port)"
  artifacts:
    - path: "src/databases/postgres.py"
      provides: "_POOL_KWARGS with statement_cache_size: 0 for Supavisor compatibility"
      contains: '"statement_cache_size": 0'
    - path: "src/services/whatsapp_bot.py"
      provides: "WhatsApp Graph API URLs at v21.0"
      contains: "graph.facebook.com/v21.0"
    - path: "src/api/routes/webhooks.py"
      provides: "WhatsApp Graph API reply URL at v21.0"
      contains: "graph.facebook.com/v21.0"
    - path: ".env.example"
      provides: "Documented placeholders for Supavisor URL, Stripe live keys, payout delay"
      contains: "STRIPE_PAYOUT_DELAY_DAYS"
    - path: "docker/staging-minimal.env"
      provides: "Comment clarifying local Postgres vs Supavisor port"
      contains: "Supavisor uses port 6543"
  key_links:
    - from: "src/databases/postgres.py _POOL_KWARGS"
      to: "asyncpg.create_pool"
      via: "kwargs spread in _asyncpg_pool_connect_kwargs"
      pattern: 'statement_cache_size":\s*0'
    - from: "src/services/whatsapp_bot.py / src/api/routes/webhooks.py"
      to: "graph.facebook.com"
      via: "HTTPS f-string URL"
      pattern: 'graph\.facebook\.com/v21\.0'
---

<objective>
Apply all Phase 1 in-repo code changes that prepare the codebase for the upcoming external service migrations:
1. Make the asyncpg pool compatible with Supabase Supavisor transaction-mode pooling (statement_cache_size=0).
2. Bring all three WhatsApp Graph API call sites to a consistent, current API version (v21.0).
3. Document new env vars in .env.example for Supabase (Supavisor URL), Stripe live mode + payout delay.
4. Clarify in docker/staging-minimal.env that the local Postgres uses port 5432 (not 6543).

Purpose: These code changes must land before the user performs the manual external upgrades (Supabase Pro, Stripe live KYC, etc.), so that when DATABASE_URL is repointed at the Supavisor pooler the app does not crash with "prepared statement already exists" errors, and so that env-var documentation is accurate for the operator.

Output: A merged code change set with five files modified. No external services contacted. Pure configuration / version-string / documentation work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-unblocked/01-CONTEXT.md
@.planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md
@.planning/phases/01-external-dependencies-unblocked/01-PATTERNS.md

@src/databases/postgres.py
@src/services/whatsapp_bot.py
@src/api/routes/webhooks.py
@.env.example
@docker/staging-minimal.env

<interfaces>
<!-- Existing interfaces the executor needs. Already in the codebase. -->
<!-- Executor should treat these as the contract — no changes to public signatures. -->

From src/databases/postgres.py (existing _POOL_KWARGS dict, lines 105-110):
```python
_POOL_KWARGS = {
    "min_size": 5,
    "max_size": 25,
    "max_inactive_connection_lifetime": 300,
    "command_timeout": 60,
}
```
This dict is spread into asyncpg.create_pool() via _asyncpg_pool_connect_kwargs(). The new key
"statement_cache_size": 0 must be added; asyncpg accepts it natively. No other code path changes.

From src/services/whatsapp_bot.py (line 78 and line 309):
```python
self.api_url = f"https://graph.facebook.com/v18.0/{settings.WHATSAPP_PHONE_ID}/messages"
# ...
status_url = f"https://graph.facebook.com/v18.0/{settings.WHATSAPP_PHONE_ID}/messages"
```

From src/api/routes/webhooks.py (line 279, inside _send_whatsapp_reply):
```python
url = f"https://graph.facebook.com/v17.0/{settings.WHATSAPP_PHONE_ID}/messages"
```

Note: the project's runtime WhatsApp webhook handler lives at src/api/routes/webhooks.py (NOT
src/api/webhooks.py). Verify with: grep -rn "graph.facebook.com" src/ — the three lines listed
above are the only matches.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add statement_cache_size=0 to asyncpg _POOL_KWARGS for Supavisor compatibility</name>
  <files>src/databases/postgres.py</files>
  <read_first>
    - src/databases/postgres.py (lines 100-180; see _POOL_KWARGS at lines 105-110 and the helpers that consume it)
    - .planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md (Section 4.3 "asyncpg Prepared Statement Incompatibility" — explains WHY this single key is required)
    - .planning/phases/01-external-dependencies-unblocked/01-PATTERNS.md (Pattern Assignment for src/databases/postgres.py)
  </read_first>
  <action>
    Edit src/databases/postgres.py: in the `_POOL_KWARGS` dict (currently at lines 105-110, four entries: min_size, max_size, max_inactive_connection_lifetime, command_timeout), append a fifth key/value pair `"statement_cache_size": 0` with an inline comment.

    The exact target state of the dict literal:
    ```python
    _POOL_KWARGS = {
        "min_size": 5,
        "max_size": 25,
        "max_inactive_connection_lifetime": 300,
        "command_timeout": 60,
        "statement_cache_size": 0,  # Required for Supabase Supavisor transaction pooler (port 6543) — disables prepared statement caching which is incompatible with transaction-mode pooling
    }
    ```

    Do NOT change any other key, value, function, or import in this file. Do NOT remove any existing column-list constants. Do NOT touch _USER_COLS / _OFFER_COLS / _CONTRACTOR_COLS / _PAYMENT_COLS.

    After saving, run:
    - `python -m ruff check src/databases/postgres.py`
    - `python -m mypy src/databases/postgres.py`

    Both must exit 0. If mypy complains about the dict type, leave the value as the literal `0` (int) — asyncpg accepts int.
  </action>
  <verify>
    <automated>grep -nE '"statement_cache_size":\s*0' src/databases/postgres.py &amp;&amp; python -m ruff check src/databases/postgres.py &amp;&amp; python -m mypy src/databases/postgres.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"statement_cache_size": 0' src/databases/postgres.py` returns at least 1
    - `grep -c 'min_size":\s*5' src/databases/postgres.py` returns at least 1 (existing key preserved)
    - `grep -c 'max_size":\s*25' src/databases/postgres.py` returns at least 1 (existing key preserved)
    - `grep -c 'command_timeout":\s*60' src/databases/postgres.py` returns at least 1 (existing key preserved)
    - `python -m ruff check src/databases/postgres.py` exits 0
    - `python -m mypy src/databases/postgres.py` exits 0
    - No other lines in postgres.py changed (verifiable via `git diff --stat src/databases/postgres.py` showing only insertions, no deletions of existing keys)
  </acceptance_criteria>
  <done>
    _POOL_KWARGS contains statement_cache_size=0 with a clear inline comment explaining the Supavisor reason. Existing keys preserved. Lint + type check pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update all three WhatsApp Graph API URLs to v21.0 (consistent current version)</name>
  <files>src/services/whatsapp_bot.py, src/api/routes/webhooks.py</files>
  <read_first>
    - src/services/whatsapp_bot.py (read lines 70-90 for the api_url assignment at line 78, then lines 300-320 for the status_url at line 309)
    - src/api/routes/webhooks.py (read lines 260-300 for the _send_whatsapp_reply function and the url f-string at line 279)
    - .planning/phases/01-external-dependencies-unblocked/01-PATTERNS.md (Pattern Assignment for whatsapp_bot.py and webhooks.py — both files share the same "WhatsApp API URL Construction Convention")
    - .planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md (Section 6.2 — confirms target version v21.0 and the deprecation timeline for v17/v18)
  </read_first>
  <action>
    Make exactly three string substitutions across two files. All three Graph API URLs MUST end up identical in version segment: `https://graph.facebook.com/v21.0/{settings.WHATSAPP_PHONE_ID}/messages`.

    File 1: src/services/whatsapp_bot.py
      - Line 78 — change `f"https://graph.facebook.com/v18.0/{settings.WHATSAPP_PHONE_ID}/messages"` to `f"https://graph.facebook.com/v21.0/{settings.WHATSAPP_PHONE_ID}/messages"`
      - Line 309 — change `f"https://graph.facebook.com/v18.0/{settings.WHATSAPP_PHONE_ID}/messages"` to `f"https://graph.facebook.com/v21.0/{settings.WHATSAPP_PHONE_ID}/messages"`

    File 2: src/api/routes/webhooks.py
      - Line 279 — change `f"https://graph.facebook.com/v17.0/{settings.WHATSAPP_PHONE_ID}/messages"` to `f"https://graph.facebook.com/v21.0/{settings.WHATSAPP_PHONE_ID}/messages"`

    Note: the path `src/api/webhooks.py` mentioned in the planning prelude does NOT exist; the actual file is `src/api/routes/webhooks.py`. Verify before editing with `grep -rn "graph.facebook.com/v" src/` — there must be exactly three matches before edits and exactly three matches after, all on `v21.0`.

    Do NOT alter the retry decorator, `_whatsapp_transient` function, error handling, or any other line. Do NOT add new imports. Do NOT change function signatures or method names.

    After saving, run:
    - `python -m ruff check src/services/whatsapp_bot.py src/api/routes/webhooks.py`
    - `python -m mypy src/services/whatsapp_bot.py src/api/routes/webhooks.py`
  </action>
  <verify>
    <automated>test "$(grep -rEc 'graph\.facebook\.com/v21\.0' src/services/whatsapp_bot.py src/api/routes/webhooks.py | awk -F: '{s+=$2} END {print s}')" = "3" &amp;&amp; test "$(grep -rEc 'graph\.facebook\.com/v(17|18|19|20)\.0' src/services/whatsapp_bot.py src/api/routes/webhooks.py | awk -F: '{s+=$2} END {print s}')" = "0" &amp;&amp; python -m ruff check src/services/whatsapp_bot.py src/api/routes/webhooks.py &amp;&amp; python -m mypy src/services/whatsapp_bot.py src/api/routes/webhooks.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "graph.facebook.com/v21.0" src/services/whatsapp_bot.py` returns 2
    - `grep -c "graph.facebook.com/v21.0" src/api/routes/webhooks.py` returns 1
    - `grep -rE "graph\.facebook\.com/v(17|18|19|20)\.0" src/` returns 0 matches (no stale versions remain anywhere in src/)
    - `python -m ruff check src/services/whatsapp_bot.py src/api/routes/webhooks.py` exits 0
    - `python -m mypy src/services/whatsapp_bot.py src/api/routes/webhooks.py` exits 0
    - `git diff --stat` for these two files shows only modifications, no new files
  </acceptance_criteria>
  <done>
    All three Graph API call sites use v21.0; no v17/v18/v19/v20 references remain anywhere under src/. Lint and type check pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document Supavisor pooler URL, Stripe live keys, and 7-day payout delay in .env.example; clarify staging-minimal.env</name>
  <files>.env.example, docker/staging-minimal.env</files>
  <read_first>
    - .env.example (read entire file — at minimum lines 25-40 for the DATABASE_URL/Supabase block, lines 100-115 for WhatsApp block, lines 140-155 for the Stripe block)
    - docker/staging-minimal.env (read entire file — line 24 has the DATABASE_URL for local Docker Postgres)
    - .planning/phases/01-external-dependencies-unblocked/01-PATTERNS.md (Pattern Assignment for .env.example and staging-minimal.env — shows exact target text for each block)
    - .planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md (Section 6.3 — env-var content; Section 4.4 — Supavisor pooler URL format)
  </read_first>
  <action>
    Make four additive edits across two files. All edits are documentation comments and new placeholder env vars. Existing variable names and values must NOT change (so the running app keeps booting with the existing test/dev values).

    File 1: .env.example

    EDIT 1 — replace the existing commented Supabase URL line (currently at line 30, reading
    `# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres`)
    with a multi-line block documenting the Supavisor transaction pooler (port 6543) and the direct connection (port 5432) for migrations:
    ```
    # Supabase Transaction Pooler (RECOMMENDED for production runtime — Supavisor, port 6543):
    # DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
    # Note: transaction-mode pooling requires statement_cache_size=0 in asyncpg pool (already set in src/databases/postgres.py)
    #
    # Supabase DIRECT connection (for Alembic migrations only — bypasses pooler, port 5432):
    # SUPABASE_DIRECT_URL=postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres
    # SUPABASE_PROJECT_REF=your-project-ref
    ```

    EDIT 2 — replace the WhatsApp block (currently at lines 106-109, the four lines from the `# WhatsApp Business API` comment through `WHATSAPP_WEBHOOK_SECRET=your-webhook-secret`) with an expanded comment block, keeping the same three variable names and placeholder values:
    ```
    # WhatsApp Business API — obtain from Meta Business Manager after account verification
    # Phone ID: numeric ID from WhatsApp Manager → Phone Numbers (NOT the E.164 phone number)
    # API Token: Bearer token from Meta App → WhatsApp → Configuration
    # Webhook Secret: chosen by you, must match the value entered in Meta Webhook setup
    WHATSAPP_API_TOKEN=your-whatsapp-api-token
    WHATSAPP_PHONE_ID=your-whatsapp-phone-id
    WHATSAPP_WEBHOOK_SECRET=your-webhook-secret
    ```

    EDIT 3 — append the following block after the existing Stripe block (which currently ends at line 149 with `STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here`). Append BEFORE the next blank line so the block is grouped with Stripe configuration:
    ```
    # Stripe LIVE mode (replace the test keys above with these after Israeli-entity KYC completion — see docs/runbooks/phase1-ops.md):
    # STRIPE_LIVE_SECRET_KEY=sk_live_your_stripe_live_secret_key_here
    # STRIPE_LIVE_PUBLISHABLE_KEY=pk_live_your_stripe_live_publishable_key_here
    # STRIPE_WEBHOOK_SECRET_LIVE=whsec_your_stripe_live_webhook_secret_here
    # STRIPE_CONTRACTOR_MEMBERSHIP_PRICE_ID=price_live_your_membership_price_id_here
    #
    # Payout timing — 7-day hold after escrow release per D-11 (Phase 1 decision); applied via
    # Stripe Connect delay_days_override in Phase 5 (D-12 deferred). Used by application code to
    # display estimated payout dates to contractors.
    STRIPE_PAYOUT_DELAY_DAYS=7
    ```

    File 2: docker/staging-minimal.env

    EDIT 4 — insert a single comment line ABOVE the existing `DATABASE_URL=...` line (currently line 24). Do NOT change the DATABASE_URL value itself (this file targets the local Docker Postgres on port 5432, not Supavisor):
    ```
    # Local Docker Postgres — uses direct port 5432. Supabase Supavisor uses port 6543 (see .env.example).
    DATABASE_URL=postgresql://postgres:local-dev-postgres-not-for-production@postgres:5432/groupio
    ```

    Do NOT remove any existing variable. Do NOT change any existing placeholder value.
  </action>
  <verify>
    <automated>grep -F "DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres" .env.example &amp;&amp; grep -F "SUPABASE_DIRECT_URL=" .env.example &amp;&amp; grep -F "SUPABASE_PROJECT_REF=" .env.example &amp;&amp; grep -F "STRIPE_LIVE_SECRET_KEY=" .env.example &amp;&amp; grep -F "STRIPE_LIVE_PUBLISHABLE_KEY=" .env.example &amp;&amp; grep -F "STRIPE_WEBHOOK_SECRET_LIVE=" .env.example &amp;&amp; grep -E "^STRIPE_PAYOUT_DELAY_DAYS=7$" .env.example &amp;&amp; grep -F "Supavisor uses port 6543" docker/staging-minimal.env</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ":6543/postgres" .env.example` returns at least 1
    - `grep -c "statement_cache_size=0 in asyncpg pool" .env.example` returns at least 1
    - `grep -c "SUPABASE_DIRECT_URL=" .env.example` returns at least 1
    - `grep -c "SUPABASE_PROJECT_REF=" .env.example` returns at least 1
    - `grep -c "STRIPE_LIVE_SECRET_KEY=" .env.example` returns at least 1
    - `grep -c "STRIPE_LIVE_PUBLISHABLE_KEY=" .env.example` returns at least 1
    - `grep -c "STRIPE_WEBHOOK_SECRET_LIVE=" .env.example` returns at least 1
    - `grep -cE "^STRIPE_PAYOUT_DELAY_DAYS=7$" .env.example` returns exactly 1 (uncommented, default value 7)
    - `grep -c "WHATSAPP_API_TOKEN=your-whatsapp-api-token" .env.example` returns exactly 1 (existing placeholder retained)
    - `grep -c "STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here" .env.example` returns exactly 1 (existing test key retained)
    - `grep -c "Supavisor uses port 6543" docker/staging-minimal.env` returns exactly 1
    - `grep -c "DATABASE_URL=postgresql://postgres:local-dev-postgres-not-for-production@postgres:5432/groupio" docker/staging-minimal.env` returns exactly 1 (existing local URL preserved unchanged)
  </acceptance_criteria>
  <done>
    .env.example documents Supavisor (6543) URL, direct migration URL, Stripe live mode keys, and STRIPE_PAYOUT_DELAY_DAYS=7. Existing test-mode keys and dev values preserved. docker/staging-minimal.env carries a clarifying comment. No production-bound runtime behaviour changed by this task — the new env vars are documented placeholders only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Postgres pool | asyncpg connection initialization (config change in pool kwargs) |
| App → Meta Graph API | Outbound HTTPS calls to graph.facebook.com (URL version segment changes) |
| Repo → operator (.env.example) | Documentation that informs how production secrets are loaded; affects what an operator pastes into a real .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | mitigate | Use `your-...-here` and commented `sk_live_...` placeholders only; never paste real keys. Acceptance criteria explicitly check that test-mode and placeholder values are retained, never replaced with live secrets. |
| T-01-02 | Tampering | src/databases/postgres.py | mitigate | The change is additive (one new dict key) and does not alter SQL generation or column lists. mypy and ruff gates run as part of the task verification. |
| T-01-03 | Denial of Service | asyncpg pool | accept | Disabling prepared-statement caching has a small per-query overhead but is REQUIRED for Supavisor; no acceptable alternative for transaction-mode pooling. Risk: low; the same setting works against direct Postgres. |
| T-01-04 | Spoofing | graph.facebook.com URL | accept | URL host pinned to graph.facebook.com over HTTPS; only the `vNN.0` path segment changes. TLS validation already enforced by httpx defaults. |
| T-01-05 | Repudiation | git history | mitigate | All three changes go through git; commit message will reference Phase 1 plan ID for traceability. |
</threat_model>

<verification>
End-to-end checks for the plan:
- `grep -rE "graph\\.facebook\\.com/v(17|18|19|20)\\.0" src/` returns 0 matches.
- `grep -c '"statement_cache_size": 0' src/databases/postgres.py` returns 1.
- `grep -F "STRIPE_PAYOUT_DELAY_DAYS=7" .env.example` matches exactly once.
- `python -m ruff check src/` exits 0.
- `python -m mypy src/databases/postgres.py src/services/whatsapp_bot.py src/api/routes/webhooks.py` exits 0.
- `pytest tests/unit/ -x -q` exits 0 (regression — no test should break since changes are additive / version-only).
</verification>

<success_criteria>
1. `_POOL_KWARGS` in src/databases/postgres.py contains `"statement_cache_size": 0` with explanatory comment.
2. All three WhatsApp Graph API URL strings (whatsapp_bot.py:78, whatsapp_bot.py:309, webhooks.py:279) use `v21.0`.
3. .env.example documents:
   - Supavisor pooler URL at port 6543 (commented placeholder)
   - SUPABASE_DIRECT_URL and SUPABASE_PROJECT_REF placeholders
   - Stripe live-mode key placeholders (commented; sk_live_, pk_live_, whsec_live_, price_live_)
   - STRIPE_PAYOUT_DELAY_DAYS=7 (uncommented, default value)
4. docker/staging-minimal.env has a comment line clarifying that local Postgres uses port 5432, not Supavisor 6543.
5. ruff and mypy pass on all modified Python files.
6. Unit tests still pass (`pytest tests/unit/ -x -q`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-external-dependencies-unblocked/01-01-SUMMARY.md` capturing:
- The exact lines changed (paths + line numbers).
- Confirmation that all three task acceptance criteria sets passed.
- Any deviation from the planned actions and why.
</output>
