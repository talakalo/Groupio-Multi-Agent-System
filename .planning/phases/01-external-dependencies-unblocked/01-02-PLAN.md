---
phase: "01"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/whatsapp-templates/groupio_offer_expiry.he.md
  - docs/whatsapp-templates/groupio_offer_expiry.en.md
  - docs/whatsapp-templates/groupio_payment_reminder.he.md
  - docs/whatsapp-templates/groupio_payment_reminder.en.md
  - docs/whatsapp-templates/groupio_payment_confirmation.he.md
  - docs/whatsapp-templates/groupio_payment_confirmation.en.md
  - docs/whatsapp-templates/groupio_offer_joined.he.md
  - docs/whatsapp-templates/groupio_offer_joined.en.md
  - docs/whatsapp-templates/groupio_contractor_matched.he.md
  - docs/whatsapp-templates/groupio_contractor_matched.en.md
  - docs/whatsapp-templates/README.md
autonomous: true
requirements:
  - OPS-01
tags:
  - whatsapp
  - templates
  - meta-business
  - i18n
  - hebrew
must_haves:
  truths:
    - "All 5 WhatsApp template names exist as reference files in both Hebrew (he) and English (en) — 10 files total"
    - "Each template file contains the exact body text the user will paste into Meta Business Manager (Utility category, {{N}} variable syntax)"
    - "Each template file declares its category (UTILITY), language code (he or en), variable list, and example values"
    - "A README in docs/whatsapp-templates/ explains how to submit templates and tracks submission status"
  artifacts:
    - path: "docs/whatsapp-templates/groupio_offer_expiry.he.md"
      provides: "Hebrew (casual) variant of offer-expiry notification"
      contains: "{{1}}"
    - path: "docs/whatsapp-templates/groupio_offer_expiry.en.md"
      provides: "English variant of offer-expiry notification"
      contains: "{{1}}"
    - path: "docs/whatsapp-templates/groupio_payment_reminder.he.md"
      provides: "Hebrew payment reminder"
      contains: "₪{{2}}"
    - path: "docs/whatsapp-templates/groupio_payment_reminder.en.md"
      provides: "English payment reminder"
      contains: "₪{{2}}"
    - path: "docs/whatsapp-templates/groupio_payment_confirmation.he.md"
      provides: "Hebrew payment confirmation"
      contains: "{{2}}"
    - path: "docs/whatsapp-templates/groupio_payment_confirmation.en.md"
      provides: "English payment confirmation"
      contains: "{{2}}"
    - path: "docs/whatsapp-templates/groupio_offer_joined.he.md"
      provides: "Hebrew welcome / joined-offer notification"
      contains: "{{3}}"
    - path: "docs/whatsapp-templates/groupio_offer_joined.en.md"
      provides: "English welcome / joined-offer notification"
      contains: "{{3}}"
    - path: "docs/whatsapp-templates/groupio_contractor_matched.he.md"
      provides: "Hebrew contractor-matched notification"
      contains: "{{2}}"
    - path: "docs/whatsapp-templates/groupio_contractor_matched.en.md"
      provides: "English contractor-matched notification"
      contains: "{{2}}"
    - path: "docs/whatsapp-templates/README.md"
      provides: "Submission process and status tracking for the 10 template files"
      contains: "Meta Business Manager"
  key_links:
    - from: "docs/whatsapp-templates/*.md"
      to: "src/services/whatsapp_bot.py send_notification(template_name=...)"
      via: "template_name string match (e.g. 'groupio_offer_expiry')"
      pattern: "groupio_(offer_expiry|payment_reminder|payment_confirmation|offer_joined|contractor_matched)"
    - from: "docs/whatsapp-templates/README.md"
      to: "Meta Business Manager (business.facebook.com)"
      via: "Submission steps"
      pattern: "business\\.facebook\\.com"
---

<objective>
Produce, in the repo, all 10 WhatsApp template reference files (5 templates × Hebrew + English) plus a README that documents the submission process. The user copies content from each file into Meta Business Manager → WhatsApp Manager → Message Templates and submits one by one.

Why in-repo: Templates are reviewed by Meta and must match exactly when called from src/services/whatsapp_bot.py via send_notification(template_name=...). Keeping the canonical body, variables, and example values in version control (a) gives the user reviewable artifacts before submission, (b) gives engineers a stable reference when wiring template calls in later phases, and (c) prevents drift between what was approved by Meta and what the code expects.

Output: 10 template `.md` files plus 1 README, total 11 files under docs/whatsapp-templates/. No code changes. No external API calls. Pure documentation deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-unblocked/01-CONTEXT.md
@.planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md

@src/services/whatsapp_bot.py

<interfaces>
<!-- Template names that send_notification() in whatsapp_bot.py will reference. -->
<!-- These exact strings (template_name) must match what is registered in Meta. -->

5 canonical template names (lowercase, snake_case, prefixed with `groupio_`):
- groupio_offer_expiry         (variables: {{1}}=offer title, {{2}}=hours remaining)
- groupio_payment_reminder     (variables: {{1}}=offer title, {{2}}=amount ILS, {{3}}=deadline date)
- groupio_payment_confirmation (variables: {{1}}=offer title, {{2}}=amount ILS)
- groupio_offer_joined         (variables: {{1}}=offer title, {{2}}=participant count, {{3}}=current price ILS)
- groupio_contractor_matched   (variables: {{1}}=offer title, {{2}}=contractor business name)

Languages: `he` (Hebrew) and `en` (English) — exact ISO codes Meta expects.
Category: UTILITY for all 10 (D-08).
Variable syntax: {{N}} sequential 1-indexed; never first or last char of body.

Existing call site (no change in this plan, just for reference):
src/services/whatsapp_bot.py exposes:
  await bot.send_notification(to=..., template_name=..., template_params=[...], language="he"|"en")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the docs/whatsapp-templates/ directory and write all 10 template reference files</name>
  <files>docs/whatsapp-templates/groupio_offer_expiry.he.md, docs/whatsapp-templates/groupio_offer_expiry.en.md, docs/whatsapp-templates/groupio_payment_reminder.he.md, docs/whatsapp-templates/groupio_payment_reminder.en.md, docs/whatsapp-templates/groupio_payment_confirmation.he.md, docs/whatsapp-templates/groupio_payment_confirmation.en.md, docs/whatsapp-templates/groupio_offer_joined.he.md, docs/whatsapp-templates/groupio_offer_joined.en.md, docs/whatsapp-templates/groupio_contractor_matched.he.md, docs/whatsapp-templates/groupio_contractor_matched.en.md</files>
  <read_first>
    - .planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md (Section 2.4 — contains the EXACT body text for all 5 templates in both languages; copy from there verbatim)
    - .planning/phases/01-external-dependencies-unblocked/01-CONTEXT.md (D-05 through D-08 — language pairs, casual Israeli tone, UTILITY category, {{N}} variable syntax)
    - src/services/whatsapp_bot.py lines 430-470 (send_notification signature — confirms how template_name and language are consumed)
  </read_first>
  <action>
    Create directory `docs/whatsapp-templates/` (use `mkdir -p docs/whatsapp-templates` first; verify the parent `docs/` already exists by listing it).

    Then create 10 files with the structure below. Each file uses one consistent template format so the user (and future automation) can read them mechanically.

    File template (substitute the marked sections per template):
    ```markdown
    # {{template_name}} ({{lang_label}})

    | Field | Value |
    |-------|-------|
    | Template name | `{{template_name}}` |
    | Category | UTILITY |
    | Language code | `{{lang_code}}` |
    | Allow category change | No (must remain UTILITY) |

    ## Body
    ```
    {{body_text}}
    ```

    ## Variables
    | # | Meaning | Example value |
    |---|---------|---------------|
    | {{1}} | {{var1_meaning}} | {{var1_example}} |
    | ...   | ...               | ...              |

    ## Submission notes
    - Submit via business.facebook.com → WhatsApp Manager → Message Templates → Create.
    - Use the example values above for the "Sample" fields Meta requires.
    - Do NOT include any URLs, prices, or promotional language beyond what is shown.
    - If Meta auto-reclassifies as MARKETING, do not accept — edit and resubmit (per D-08).
    ```

    Now the EXACT content per file. Body text is verbatim from RESEARCH.md Section 2.4 — DO NOT paraphrase, do NOT translate, do NOT add or remove emojis.

    --- File 1: docs/whatsapp-templates/groupio_offer_expiry.he.md ---
      template_name: groupio_offer_expiry
      lang_label: Hebrew (קצב יומיומי, casual Israeli)
      lang_code: he
      body_text:
        ```
        היי! ההצעה *{{1}}* תפוג בעוד {{2}} שעות 🔔
        עוד שכנים מצטרפים — המחיר יורד.
        כדי לא להחמיץ: groupio.co.il
        ```
      Variables table:
        - {{1}} | offer title | "מיזוג אוויר מרכזי"
        - {{2}} | hours remaining | "24"

    --- File 2: docs/whatsapp-templates/groupio_offer_expiry.en.md ---
      template_name: groupio_offer_expiry
      lang_label: English
      lang_code: en
      body_text:
        ```
        Hey! The offer *{{1}}* expires in {{2}} hours 🔔
        More neighbors joining means a lower price.
        Don't miss out: groupio.co.il
        ```
      Variables table:
        - {{1}} | offer title | "Central A/C installation"
        - {{2}} | hours remaining | "24"

    --- File 3: docs/whatsapp-templates/groupio_payment_reminder.he.md ---
      template_name: groupio_payment_reminder
      lang_label: Hebrew (casual)
      lang_code: he
      body_text:
        ```
        תזכורת: תשלום של ₪{{2}} להצעה *{{1}}* מגיע עד {{3}}.
        השלם את התשלום כדי להבטיח את מקומך בקבוצה.
        ```
      Variables:
        - {{1}} | offer title | "התקנת מזגן"
        - {{2}} | amount in ILS | "450"
        - {{3}} | deadline date | "15/06/2026"

    --- File 4: docs/whatsapp-templates/groupio_payment_reminder.en.md ---
      template_name: groupio_payment_reminder
      lang_label: English
      lang_code: en
      body_text:
        ```
        Reminder: Payment of ₪{{2}} for offer *{{1}}* is due by {{3}}.
        Complete your payment to secure your spot in the group.
        ```
      Variables:
        - {{1}} | offer title | "AC installation"
        - {{2}} | amount in ILS | "450"
        - {{3}} | deadline date | "15/06/2026"

    --- File 5: docs/whatsapp-templates/groupio_payment_confirmation.he.md ---
      template_name: groupio_payment_confirmation
      lang_label: Hebrew (casual)
      lang_code: he
      body_text:
        ```
        קיבלנו את התשלום שלך ✅
        *{{1}}* — ₪{{2}} שולם בהצלחה.
        נעדכן אותך כשיהיה עדכון מהקבלן.
        ```
      Variables:
        - {{1}} | offer title | "שיפוץ לובי"
        - {{2}} | amount in ILS | "320"

    --- File 6: docs/whatsapp-templates/groupio_payment_confirmation.en.md ---
      template_name: groupio_payment_confirmation
      lang_label: English
      lang_code: en
      body_text:
        ```
        We received your payment ✅
        *{{1}}* — ₪{{2}} paid successfully.
        We'll update you when there's news from the contractor.
        ```
      Variables:
        - {{1}} | offer title | "Lobby renovation"
        - {{2}} | amount in ILS | "320"

    --- File 7: docs/whatsapp-templates/groupio_offer_joined.he.md ---
      template_name: groupio_offer_joined
      lang_label: Hebrew (casual)
      lang_code: he
      body_text:
        ```
        הצטרפת בהצלחה ל-*{{1}}* 🎉
        כרגע {{2}} שכנים בקבוצה — המחיר הנוכחי: ₪{{3}} לדירה.
        ככל שיצטרפו יותר, המחיר ירד עוד!
        ```
      Variables:
        - {{1}} | offer title | "חשמלאי לבניין"
        - {{2}} | participant count | "8"
        - {{3}} | current price ILS | "280"

    --- File 8: docs/whatsapp-templates/groupio_offer_joined.en.md ---
      template_name: groupio_offer_joined
      lang_label: English
      lang_code: en
      body_text:
        ```
        You've joined *{{1}}* successfully 🎉
        {{2}} neighbors in the group — current price: ₪{{3}} per unit.
        The more who join, the lower the price!
        ```
      Variables:
        - {{1}} | offer title | "Building electrician"
        - {{2}} | participant count | "8"
        - {{3}} | current price ILS | "280"

    --- File 9: docs/whatsapp-templates/groupio_contractor_matched.he.md ---
      template_name: groupio_contractor_matched
      lang_label: Hebrew (casual)
      lang_code: he
      body_text:
        ```
        מצאנו קבלן להצעה *{{1}}* 🔨
        *{{2}}* ייצור איתכם קשר לתיאום.
        לפרטים נוספים: groupio.co.il
        ```
      Variables:
        - {{1}} | offer title | "אינסטלציה לבניין"
        - {{2}} | contractor business name | "אלקטרו מאיר בע״מ"

    --- File 10: docs/whatsapp-templates/groupio_contractor_matched.en.md ---
      template_name: groupio_contractor_matched
      lang_label: English
      lang_code: en
      body_text:
        ```
        We found a contractor for *{{1}}* 🔨
        *{{2}}* will contact you to coordinate.
        More details: groupio.co.il
        ```
      Variables:
        - {{1}} | offer title | "Building plumbing"
        - {{2}} | contractor business name | "Electro Meir Ltd."

    All Hebrew text MUST be saved as UTF-8 (this is the project default — verify by re-reading one Hebrew file after writing and confirming the Hebrew renders, not mojibake). Do NOT add a BOM. Do NOT word-wrap inside the body code block — keep line breaks as shown.
  </action>
  <verify>
    <automated>test "$(ls docs/whatsapp-templates/*.md 2>/dev/null | grep -E '/groupio_(offer_expiry|payment_reminder|payment_confirmation|offer_joined|contractor_matched)\.(he|en)\.md$' | wc -l)" = "10" &amp;&amp; for f in docs/whatsapp-templates/groupio_offer_expiry.he.md docs/whatsapp-templates/groupio_offer_expiry.en.md docs/whatsapp-templates/groupio_payment_reminder.he.md docs/whatsapp-templates/groupio_payment_reminder.en.md docs/whatsapp-templates/groupio_payment_confirmation.he.md docs/whatsapp-templates/groupio_payment_confirmation.en.md docs/whatsapp-templates/groupio_offer_joined.he.md docs/whatsapp-templates/groupio_offer_joined.en.md docs/whatsapp-templates/groupio_contractor_matched.he.md docs/whatsapp-templates/groupio_contractor_matched.en.md; do grep -q "Category | UTILITY" "$f" || { echo "MISSING UTILITY in $f"; exit 1; }; grep -q "{{1}}" "$f" || { echo "MISSING {{1}} in $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/whatsapp-templates/ | grep -cE 'groupio_(offer_expiry|payment_reminder|payment_confirmation|offer_joined|contractor_matched)\.(he|en)\.md$'` returns exactly 10
    - For each of the 10 files: `grep -c "Category | UTILITY" {file}` returns at least 1
    - For each of the 10 files: `grep -c "{{1}}" {file}` returns at least 1 (every template has at least one variable)
    - `grep -F "₪{{2}}" docs/whatsapp-templates/groupio_payment_reminder.he.md` matches (Hebrew body has the exact ₪{{2}} amount placeholder)
    - `grep -F "₪{{2}}" docs/whatsapp-templates/groupio_payment_reminder.en.md` matches (English body matches)
    - `grep -F "{{3}}" docs/whatsapp-templates/groupio_offer_joined.he.md` matches (3-variable template has all three placeholders)
    - `grep -F "{{3}}" docs/whatsapp-templates/groupio_offer_joined.en.md` matches
    - `grep -F "{{3}}" docs/whatsapp-templates/groupio_payment_reminder.he.md` matches
    - `grep -F "groupio.co.il" docs/whatsapp-templates/groupio_offer_expiry.he.md` matches (Hebrew body has the URL)
    - `grep -F "groupio.co.il" docs/whatsapp-templates/groupio_offer_expiry.en.md` matches
    - `grep -F "Language code | \`he\`" docs/whatsapp-templates/groupio_offer_expiry.he.md` matches
    - `grep -F "Language code | \`en\`" docs/whatsapp-templates/groupio_offer_expiry.en.md` matches
    - At least one Hebrew file contains real Hebrew (UTF-8): `grep -E "[\\xd7][\\x90-\\xaa]" docs/whatsapp-templates/groupio_offer_expiry.he.md` exits 0 (matches Unicode Hebrew letter range encoded as UTF-8 bytes)
  </acceptance_criteria>
  <done>
    All 10 files exist, each declares UTILITY category and language code, each contains the verbatim body from RESEARCH.md Section 2.4, and each lists every variable placeholder with an example value. Hebrew renders correctly in UTF-8.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the docs/whatsapp-templates/README.md submission guide and status tracker</name>
  <files>docs/whatsapp-templates/README.md</files>
  <read_first>
    - .planning/phases/01-external-dependencies-unblocked/01-RESEARCH.md (Section 2.1 Meta Business Account prerequisite, Section 2.2 template technical requirements, Section 2.3 submission steps)
    - All 10 files created in Task 1 (read each filename via `ls docs/whatsapp-templates/`)
    - .planning/phases/01-external-dependencies-unblocked/01-CONTEXT.md (D-05 through D-08 for the policy summary)
  </read_first>
  <action>
    Create `docs/whatsapp-templates/README.md` with the following exact structure. Substitute today's date `2026-05-07` in the "Last updated" footer.

    ```markdown
    # WhatsApp Business Templates — Submission Reference

    This directory holds the canonical content of every WhatsApp Business message template
    Groupio submits to Meta. Each `groupio_*.{he|en}.md` file is the source of truth for
    the body, variables, and example values that must match exactly what is registered in
    Meta Business Manager.

    The templates are referenced from `src/services/whatsapp_bot.py` via
    `bot.send_notification(template_name=..., template_params=[...], language="he"|"en")`.
    The `template_name` arg MUST match the Meta-approved template name; `language` MUST
    match the language code on the corresponding `.{he|en}.md` file.

    ## Policy summary (locked decisions)

    | Decision | Value |
    |----------|-------|
    | D-05 — Languages | Hebrew + English; submit each as a separate language variant |
    | D-06 — Hebrew tone | Casual Israeli (יומיומי), WhatsApp-native |
    | D-07 — Drafting | All 5 templates drafted in this repo for user review before Meta submission |
    | D-08 — Category | UTILITY for all (never MARKETING); `{{N}}` variable syntax; no promotional language |

    ## Submission process (per template, per language — repeat 10 times)

    1. Open business.facebook.com → WhatsApp Manager → Message Templates.
    2. Click **Create template**.
    3. Name (lowercase, snake_case): copy from the `Template name` row in the file.
    4. Category: **Utility** (do NOT allow auto-reclassification to Marketing).
    5. Language: copy from the `Language code` row (`he` or `en`).
    6. Body: copy the body code block verbatim from the file. Do not paraphrase.
    7. Sample values: paste the example values from the Variables table.
    8. Submit for review. Record the submission timestamp + Meta reference ID in the
       Submission Status table below.

    ## The 5 templates × 2 languages = 10 submissions

    | # | Template | Language | File | Status | Meta ref ID | Submitted | Approved |
    |---|----------|----------|------|--------|-------------|-----------|----------|
    | 1 | groupio_offer_expiry         | he | [groupio_offer_expiry.he.md](groupio_offer_expiry.he.md)               | Pending | — | — | — |
    | 2 | groupio_offer_expiry         | en | [groupio_offer_expiry.en.md](groupio_offer_expiry.en.md)               | Pending | — | — | — |
    | 3 | groupio_payment_reminder     | he | [groupio_payment_reminder.he.md](groupio_payment_reminder.he.md)       | Pending | — | — | — |
    | 4 | groupio_payment_reminder     | en | [groupio_payment_reminder.en.md](groupio_payment_reminder.en.md)       | Pending | — | — | — |
    | 5 | groupio_payment_confirmation | he | [groupio_payment_confirmation.he.md](groupio_payment_confirmation.he.md) | Pending | — | — | — |
    | 6 | groupio_payment_confirmation | en | [groupio_payment_confirmation.en.md](groupio_payment_confirmation.en.md) | Pending | — | — | — |
    | 7 | groupio_offer_joined         | he | [groupio_offer_joined.he.md](groupio_offer_joined.he.md)               | Pending | — | — | — |
    | 8 | groupio_offer_joined         | en | [groupio_offer_joined.en.md](groupio_offer_joined.en.md)               | Pending | — | — | — |
    | 9 | groupio_contractor_matched   | he | [groupio_contractor_matched.he.md](groupio_contractor_matched.he.md)   | Pending | — | — | — |
    | 10| groupio_contractor_matched   | en | [groupio_contractor_matched.en.md](groupio_contractor_matched.en.md)   | Pending | — | — | — |

    Status values: `Pending` → `Submitted` → `Approved` | `Rejected (reason)`.

    ## Common rejection causes (avoid these)

    - URL or "click here" CTAs that look promotional → Meta reclassifies as MARKETING.
    - Variables `{{N}}` as the first or last character of the body (always wrap with text).
    - Mismatched braces (`{1}` instead of `{{1}}`).
    - Body language doesn't match declared language code.
    - Missing example values for any variable.

    ## Verification once approved

    Send a real test message via the WhatsApp Business API to a verified test phone:
    ```python
    from src.services.whatsapp_bot import bot
    await bot.send_notification(
        to="+972501234567",
        template_name="groupio_payment_confirmation",
        template_params=["שיפוץ לובי", "320"],
        language="he",
    )
    ```
    Confirm the message arrives on the test phone before relying on it in production
    notification flows. (Phase 9 success criterion 4 references this same test.)

    ---
    *Last updated: 2026-05-07*
    ```

    Verify after writing: open the file, count rows in the table (must be 10 data rows + 1 header + 1 separator), and ensure every relative link target file exists.
  </action>
  <verify>
    <automated>test -f docs/whatsapp-templates/README.md &amp;&amp; grep -cE '^\| *[0-9]+ *\| *groupio_' docs/whatsapp-templates/README.md | grep -q "^10$" &amp;&amp; grep -q "business\.facebook\.com" docs/whatsapp-templates/README.md &amp;&amp; grep -q "Category | UTILITY" docs/whatsapp-templates/README.md || grep -q "Utility" docs/whatsapp-templates/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/whatsapp-templates/README.md` succeeds
    - `grep -cE '^\| *[0-9]+ *\| *groupio_' docs/whatsapp-templates/README.md` returns exactly 10 (10 numbered rows in submission table)
    - `grep -c "business.facebook.com" docs/whatsapp-templates/README.md` returns at least 1
    - `grep -c "Utility" docs/whatsapp-templates/README.md` returns at least 1 (category referenced)
    - `grep -c "groupio_offer_expiry" docs/whatsapp-templates/README.md` returns at least 2 (he and en rows)
    - `grep -c "groupio_payment_reminder" docs/whatsapp-templates/README.md` returns at least 2
    - `grep -c "groupio_payment_confirmation" docs/whatsapp-templates/README.md` returns at least 2
    - `grep -c "groupio_offer_joined" docs/whatsapp-templates/README.md` returns at least 2
    - `grep -c "groupio_contractor_matched" docs/whatsapp-templates/README.md` returns at least 2
    - Every linked file actually exists: `for f in $(grep -oE '\(groupio_[a-z_]+\.(he|en)\.md\)' docs/whatsapp-templates/README.md | tr -d '()'); do test -f "docs/whatsapp-templates/$f" || exit 1; done`
  </acceptance_criteria>
  <done>
    README documents the policy, submission steps, common rejection causes, the 10-row submission tracker, and the verification snippet. All 10 relative links resolve to existing files in the same directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → operator | Templates are read by a human, copy-pasted into Meta Business Manager UI |
| Repo → src/services/whatsapp_bot.py | Template names referenced by string match; misspelling causes silent send failure (handled by existing code paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | docs/whatsapp-templates/*.md | accept | Documentation files only; reviewed in PR; no runtime trust placed on file contents until a human submits to Meta. |
| T-01-07 | Information Disclosure | template body | accept | Bodies contain no PII; only the domain `groupio.co.il` and standard transactional phrasing. |
| T-01-08 | Spoofing | template name string | mitigate | Acceptance criteria verify exact string `groupio_<purpose>` in each filename and inside file body, preventing accidental rename. Future code that calls `bot.send_notification(template_name=...)` MUST use the exact same names. |
| T-01-09 | Repudiation | submission status | mitigate | README maintains the audit trail (status, ref ID, submitted, approved columns) so the operator can record Meta's response per template. |
</threat_model>

<verification>
- `ls docs/whatsapp-templates/ | wc -l` returns 11 (10 template files + README.md).
- All 5 template names appear at least twice each (he + en) in `docs/whatsapp-templates/README.md`.
- `grep -rEc "Category \| UTILITY" docs/whatsapp-templates/*.md | awk -F: '{s+=$2} END {print s}'` is at least 10 (one per template file; README may have additional matches).
- A spot-read of any `.he.md` file shows readable Hebrew (not mojibake) confirming UTF-8.
- Every linked filename in README.md exists in the directory.
</verification>

<success_criteria>
1. 10 template `.md` files exist under `docs/whatsapp-templates/` with correct naming convention `{template_name}.{he|en}.md`.
2. Each file declares: template name, UTILITY category, language code, body code block (verbatim from RESEARCH.md §2.4), variables table with example values, submission notes.
3. `docs/whatsapp-templates/README.md` exists with a 10-row Submission Status tracker, links to each file, the submission steps, and rejection-causes guidance.
4. All Hebrew text is valid UTF-8 (no mojibake).
5. The 5 template names match exactly: groupio_offer_expiry, groupio_payment_reminder, groupio_payment_confirmation, groupio_offer_joined, groupio_contractor_matched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-external-dependencies-unblocked/01-02-SUMMARY.md` capturing:
- The list of 11 files created.
- Confirmation that each Hebrew file renders correctly (paste a 1-line excerpt for proof).
- Note that the user must now manually submit each template via business.facebook.com.
</output>
