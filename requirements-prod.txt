# Production-only dependencies (no pytest, ruff, mypy).
# Use this in Docker to keep image smaller.
# Full list including dev: requirements.txt

# Core framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# AI & LLM (langchain-community omitted: not imported in src, saves ~300MB+)
anthropic>=0.39.0
openai>=1.6.0
langchain>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.30
tiktoken>=0.5.0

# Databases
alembic>=1.13.0
# Alembic uses SQLAlchemy create_engine with postgresql:// — defaults to psycopg2 (sync).
# App runtime uses asyncpg (async). Both connect to the same Postgres.
psycopg2-binary>=2.9.0
qdrant-client>=1.7.0
neo4j>=5.15.0
supabase>=2.3.0
asyncpg>=0.29.0
redis[hiredis]>=5.0.0
aio-pika>=9.4.0

# HTTP & Networking
aiosmtplib>=3.0.0
httpx>=0.25.0
python-multipart>=0.0.6
tenacity>=8.2.0

# Monitoring
sentry-sdk[fastapi]>=1.39.0
prometheus-client>=0.19.0
structlog>=23.2.0

# Auth
PyJWT>=2.8.0
bcrypt>=4.0.0

# Utilities
numpy>=1.26.0
